---
# Compatible with AWS images of Debian and Ubuntu
fun_debian_common_admin_user: "{{ 'admin' if (ansible_distribution == 'Debian') else 'ubuntu' }}"
fun_debian_common_regular_user_group: "{{ fun_debian_common_regular_user if (fun_debian_common_regular_user is defined) else '' }}"

# Time zone
fun_debian_common_ntp_timezone: Europe/Paris

# UFW
fun_debian_common_firewall: yes

# Common packages
fun_debian_common_packages:
  - atop
  - bash-completion
  - apt-transport-https
  - ca-certificates
  - software-properties-common
  - curl
  - tmux
  - tree
  - vim
  - git
  - htop
  - acl
  - dbus

# Extra and supplementary packages
fun_debian_common_extra_packages:
  - libgmp-dev
  - python3-pip
  - virtualenv
  - python3-setuptools
  - build-essential
  - dkms
  - dnsutils
  - gnupg2
  - net-tools
  - nmap
  - unzip

fun_debian_common_supplementary_packages: []

# Bash history configuration
fun_debian_common_bash_history: true
fun_debian_common_bash_history_histsize: "5000"
fun_debian_common_bash_history_histfilesize: "3000"
fun_debian_common_bash_history_histtimeformat: "%c%t"
