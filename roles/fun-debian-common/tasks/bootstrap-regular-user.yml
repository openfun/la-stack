---
- name: Create regular user group
  group:
    name: "{{ fun_debian_common_regular_user_group }}"
    state: present

- name: Create regular user
  user:
    name: "{{ fun_debian_common_regular_user }}"
    comment: "Unprivileged {{ fun_debian_common_regular_user }} user"
    group: "{{ fun_debian_common_regular_user_group }}"
    shell: /bin/bash

- import_tasks: set-authorized-keys-user.yml
